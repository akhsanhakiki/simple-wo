---
import Layout from '../layouts/Layout.astro';
import GuestManager from '../components/GuestManager';
---

<Layout>
	{/* client:only — HeroUI Select/ComboBox (React Aria ListBox) require client context; SSR throws selectionManager null */}
	<GuestManager client:only="react" />
</Layout>
